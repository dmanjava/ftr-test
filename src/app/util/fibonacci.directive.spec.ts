import { FibonacciDirective } from './fibonacci.directive';

describe('FibonacciDirective', () => {
  it('should create an instance', () => {
    const directive = new FibonacciDirective();
    expect(directive).toBeTruthy();
  });
});
